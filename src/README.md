# Legacy Node.js Source Code

> **Note:** This directory contains the original Node.js implementation which has been superseded by the Rust implementation. It is kept for reference only.
>
> The active server code is in [`../letta-server/`](../letta-server/) (Rust). See the [main README](../README.md) for current usage.