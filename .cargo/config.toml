# Dev build optimizations for faster iteration
# mold linker is ~5-10x faster than the default ld for linking
[target.x86_64-unknown-linux-gnu]
linker = "clang"
rustflags = ["-C", "link-arg=-fuse-ld=mold"]
